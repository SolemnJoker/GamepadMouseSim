# 手柄鼠标模拟器 - 测试指南

## 运行

```bash
cd D:\project\sbgj\build
.\GamepadMouseSim.exe
```

---

## 设计理念

- **LT = Alt修饰键**：按住LT时Alt被按住，用于窗口切换、关闭窗口等
- **RT = Ctrl修饰键**：按住RT时Ctrl被按住，用于复制粘贴、标签切换等
- **Y = 自由键**：单按为Enter，不作为修饰键

---

## 手柄按键说明

```
        [LB]                    [RB]
         ●                       ●
    [LT]━━━━━━━━━━━━━━━━━━━━━━━━━[RT]
              ┌─────────┐
   [View] ●  │         │  ● [Menu]
    (Back)   │  [摇杆]  │   (Start)
             │  L3  R3 │
    [↑]      │         │      [Y]
   [←][→]  [L摇杆]   [R摇杆]  [X]
    [↓]                  [A][B]
```

| 按键 | 位置 |
|------|------|
| A/B/X/Y | 右侧四个彩色按钮 |
| LB/RB | 手柄上方两个肩键 |
| LT/RT | LB/RB下方的扳机键（可按压） |
| L3/R3 | 按下左右摇杆本身 |
| View | 手柄中间偏左小按钮（两个方框图标） |
| Menu | 手柄中间偏右小按钮（三条横线图标） |
| 十字键 | 手柄左下方的Dpad |

---

## 基础层（直接按键）

| 按键 | 效果 |
|------|------|
| A | 鼠标左键 |
| B | 鼠标右键 |
| X | 鼠标中键 |
| Y | Enter（回车） |
| LB | 按住左键（拖拽用） |
| RB | 按住右键 |
| L3 | Enter（回车） |
| R3 | Escape（退出） |
| View | Tab |
| Menu | Win键（开始菜单） |
| 左摇杆 | 移动鼠标 |
| 右摇杆 | 滚轮 |
| Dpad ↑ | 滚轮上 |
| Dpad ↓ | 滚轮下 |

---

## LT修饰层（按住LT时Alt被按住）

**使用场景**：窗口切换、文本导航、常用快捷键

| 按住LT + 按 | 效果 | 场景 |
|-------------|------|------|
| Dpad → | Tab | 在Alt+Tab窗口中向右切换 |
| Dpad ← | Shift+Tab | 在Alt+Tab窗口中向左切换 |
| Dpad ↑ | 方向键↑ | 向上选择 |
| Dpad ↓ | 方向键↓ | 向下选择 |
| A | Enter | 确认选择 |
| B | Escape | 取消 |
| X | Alt+Tab | 打开窗口切换器 |
| Y | Alt+F4 | 关闭当前窗口 |
| LB | Ctrl+A | 全选 |
| RB | Ctrl+C | 复制 |
| L3 | Ctrl+V | 粘贴 |
| R3 | Ctrl+Z | 撤销 |

### LT使用示例

**切换窗口**：按住LT + 按X → 松开LT → 窗口切换器打开 → 按住LT + Dpad→循环 → 按A确认

**复制粘贴**：按住LT + 按RB(复制) → 移动光标 → 按住LT + 按L3(粘贴)

---

## RT修饰层（按住RT时Ctrl被按住）

**使用场景**：浏览器标签、文档编辑、保存

| 按住RT + 按 | 效果 | 场景 |
|-------------|------|------|
| Dpad → | Ctrl+Tab | 切换到下一个标签 |
| Dpad ← | Ctrl+Shift+Tab | 切换到上一个标签 |
| Dpad ↑ | Page Up | 向上翻页 |
| Dpad ↓ | Page Down | 向下翻页 |
| A | Ctrl+A | 全选 |
| B | Ctrl+C | 复制 |
| X | Ctrl+X | 剪切 |
| Y | Ctrl+V | 粘贴 |
| RB | Ctrl+S | 保存 |
| L3 | Ctrl+Z | 撤销 |
| R3 | Ctrl+Shift+Z | 重做 |

### RT使用示例

**切换浏览器标签**：按住RT + 按Dpad→ → 松开RT

**保存文件**：按住RT + 按RB → 松开RT

---

## 模式切换

| 操作 | 效果 |
|------|------|
| 同时按住 View + Menu 1秒 | 切换鼠标模式 ↔ 默认模式 |
| 右键托盘图标 → Switch Mode | 手动切换 |

---

## 常用操作速查

| 想做什么 | 手柄操作 |
|---------|---------|
| 点击 | A(左键) / B(右键) |
| 拖拽 | 按住LB + 推左摇杆 |
| 滚动 | 推右摇杆 |
| 回车确认 | Y 或 L3 |
| 退出/取消 | R3 |
| 切换窗口 | 按住LT + 按X → 松开LT |
| 在切换窗口中循环 | 按住LT + Dpad→/← |
| 关闭窗口 | 按住LT + 按Y |
| 复制 | 按住LT + 按RB |
| 粘贴 | 按住LT + 按L3 |
| 全选 | 按住LT + 按LB |
| 撤销 | 按住LT + 按R3 |
| 切换浏览器标签 | 按住RT + Dpad→/← |
| 保存 | 按住RT + 按RB |
| 翻页 | 按住RT + Dpad↑/↓ |
