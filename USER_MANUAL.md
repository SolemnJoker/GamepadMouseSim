# 手柄鼠标模拟器 - 使用说明书

## 快速开始

1. 插入Xbox手柄（兼容XInput的第三方手柄也可）
2. 运行 `GamepadMouseSim.exe`
3. 程序自动最小化到**系统托盘**（右下角图标区）
4. 程序默认进入**鼠标模式**，手柄按键被映射为鼠标/键盘操作

> **提示**：启动时如果Windows弹出安全警告，点击"更多信息"→"仍要运行"即可

---

## 两种工作模式

| 模式 | 什么时候用 | 手柄行为 | 托盘图标 |
|------|-----------|---------|--------|
| **鼠标模式** | 桌面操作、浏览网页、编辑文档 | 手柄按键映射为鼠标/键盘 | 🖱 鼠标图标 |
| **默认模式** | 玩游戏 | 手柄信号原样传给游戏 | 🎮 手柄图标 |

### 自动切换

程序每2秒检测一次系统进程，如果发现配置列表中的游戏进程正在运行（如steam.exe），自动切换到默认模式。游戏关闭后自动切回鼠标模式。

### 手动切换

**同时按住 View键 + Menu键 1秒**即可切换模式。

---

## 按键操作指南

### 基础操作（不需要按住任何修饰键）

| 手柄按键 | 效果 | 常用场景 |
|---------|------|---------|
| A | 鼠标左键单击 | 点击按钮、链接 |
| B | 鼠标右键单击 | 弹出右键菜单 |
| X | 鼠标中键单击 | 关闭浏览器标签页 |
| Y | 回车键(Enter) | 确认 |
| LB | 按住鼠标左键 | 配合左摇杆拖拽文件 |
| RB | 按住鼠标右键 | 右键拖拽 |
| L3（按下左摇杆） | 回车(Enter) | 确认 |
| R3（按下右摇杆） | 退出(Escape) | 关闭弹窗、取消 |
| View | Tab键 | 切换焦点 |
| Menu | Win键 | 打开开始菜单 |
| 左摇杆 | 移动鼠标光标 | 轻推慢速、重推快速 |
| 右摇杆 | 上下/左右滚动 | 浏览网页、文档 |
| Dpad ↑ | 音量增大 | |
| Dpad ↓ | 音量减小 | |
| Dpad ← | 静音/取消静音 | |

### 键盘编辑操作（按住LT扳机）

| 操作 | 效果 | 常用场景 |
|------|------|---------|
| 按住LT + 按X | 打开Alt+Tab窗口切换器 | 切换应用程序 |
| 按住LT + 按LB | 在切换器中向左选 | Alt+Shift+Tab |
| 按住LT + 按RB | 在切换器中向右选 | Alt+Tab前进 |
| 按住LT + 按A | 回车确认 | 确认选择 |
| 按住LT + 按B | Escape取消 | 关闭菜单 |
| 按住LT + 按Y | 关闭当前窗口(Alt+F4) | 关闭程序 |
| 按住LT + 按L3 | 粘贴(Ctrl+V) | |
| 按住LT + 按R3 | 撤销(Ctrl+Z) | |

### 浏览器/文档操作（按住RT扳机）

| 操作 | 效果 | 常用场景 |
|------|------|---------|
| 按住RT + Dpad→ | 切换到下一个标签(Ctrl+Tab) | 浏览器 |
| 按住RT + Dpad← | 切换到上一个标签(Ctrl+Shift+Tab) | 浏览器 |
| 按住RT + Dpad↑ | 向上翻页(PageUp) | 浏览文档 |
| 按住RT + Dpad↓ | 向下翻页(PageDown) | 浏览文档 |
| 按住RT + 按A | 全选(Ctrl+A) | |
| 按住RT + 按B | 复制(Ctrl+C) | |
| 按住RT + 按X | 剪切(Ctrl+X) | |
| 按住RT + 按Y | 粘贴(Ctrl+V) | |
| 按住RT + 按RB | 保存(Ctrl+S) | 保存文档 |
| 按住RT + 按L3 | 撤销(Ctrl+Z) | |
| 按住RT + 按R3 | 重做(Ctrl+Shift+Z) | |

---

## 窗口切换操作示例

**场景**：在浏览器和记事本之间切换

1. **按住LT扳机不放手**
2. **按一下X** → 屏幕弹出Alt+Tab窗口切换器
3. **按LB向左选**或**按RB向右选** → 在打开的窗口中循环
4. **松开LT或按A** → 确认切换到选中的窗口

---

## 系统托盘操作

右键点击托盘图标，弹出菜单：

| 菜单项 | 功能 |
|--------|------|
| Mode: xxx | 显示当前模式（灰色不可点击） |
| Switch Mode | 手动切换鼠标/默认模式 |
| Lock Mode | 锁定当前模式（禁自动检测） |
| Pause Passthrough | 暂停所有手柄映射 |
| Exit | 退出程序 |

**双击托盘图标**可快速切换模式。

---

## 配置文件

配置文件位于程序同目录下的 `config.json`。

首次运行自动生成默认配置文件。你可以用记事本打开编辑：

```json
{
  "monitoring": {
    "process_list": ["steam.exe", "你的游戏.exe"],
    "polling_interval_seconds": 2,
    "manual_switch_lockout_seconds": 30
  },
  "mouse_mode": {
    "left_stick": {
      "sensitivity_x": 1.0,
      "sensitivity_y": 1.0,
      "deadzone": 0.15
    }
  }
}
```

**热加载**：修改配置后保存，程序自动生效，无需重启。

### 常用配置调整

| 配置项 | 作用 | 建议值 |
|--------|------|--------|
| process_list | 自动检测的游戏进程名 | 添加你的游戏.exe |
| polling_interval_seconds | 进程检测间隔 | 1-10秒 |
| sensitivity_x/y | 鼠标移动灵敏度 | 0.5-2.0 |
| deadzone | 摇杆死区(越小越灵敏) | 0.10-0.20 |
| acceleration | 是否启用鼠标加速 | true/false |

---

## 常见问题

**Q: 程序无法启动，Windows提示安全问题？**
A: 这是自编译程序的正常现象。点击"更多信息"→"仍要运行"。如果无法通过，可在Windows安全中心关闭"智能应用控制"。

**Q: 手柄没反应？**
A: 确保手柄已连接且支持XInput（Xbox手柄或兼容的第三方手柄）。查看托盘图标是否显示手柄图标。

**Q: 如何只在玩游戏时才关闭映射？**
A: 程序会自动检测。把你的游戏进程名加到config.json的`process_list`中即可。

**Q: 如何关闭程序自启动？**
A: 编辑config.json，将`autostart`设为`false`。

**Q: 如何查看调试日志？**
A: 运行目录下的`debug.log`文件记录了程序运行日志。
